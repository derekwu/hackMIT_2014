function play_board(board) {
	
	
	
	
	board_size = board.size
	for (i = 0; i < board_size; i++) {
		
        for (j = 0; j < board_size; j++) {
			if (board[i][j] == 1) {
				
			}
            board[i][j] = 
    }
}

function init_notes() {
	
}